<% form_tag({}, :method => :get) do %>
  <%= select_tag :indicator, options_for_select(indicator_list) %>
  <%= submit_tag 'Go' %>
<% end %>

<%= render :partial => 'map', :locals => {:map => resource } %>
<% if is_admin? %>
<%= link_to 'Edit', edit_map_path(@map) %>
<% end %>
