<h1>Listing graphs</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Units</th>
    <th>Vertical label</th>
    <th>Horizontal label</th>
  </tr>

<% @graphs.each do |graph| %>
  <tr>
    <td><%=h graph.title %></td>
    <td><%=h graph.units %></td>
    <td><%=h graph.vertical_label %></td>
    <td><%=h graph.horizontal_label %></td>
    <td><%= link_to 'Show', graph %></td>
    <td><%= link_to 'Edit', edit_graph_path(graph) %></td>
    <td><%= link_to 'Destroy', graph, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New graph', new_graph_path %>